#include "00 Definition.h"
#include "01 SequentialList 顺序表.h"

int main() {
    SequentialList Sequential_one;
    SequenceList_Initiate(&Sequential_one);
    SequenceList_InsertElement(&Sequential_one, 1, 648);
    SequentialList_GetSequentialListLength(Sequential_one);
    SequenceList_PrintList(Sequential_one);
    SequenceList_DeleteElement(&Sequential_one, 1, )
}
